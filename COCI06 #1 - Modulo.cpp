#include <bits/stdc++.h>

#define in(y) insert(y);
#define si set<int>                      

using namespace std;

int main(){
    cin.tie(0);
    cout.tie(0);
    int x;
    si k;
    while(r--){
    	cin>>x;
    	k.in(x%42);
    }
    cout<<k.size()<<"\n";
    
    return 0;
}
